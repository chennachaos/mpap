
#include "Macro.h"
//#include "Plot.h"


////extern Plot plot;


int macro50(Macro &macro)
{
/*
  if (!macro) 
  { 
    macro.name = "fram";
    macro.type = "plot";
    macro.what = "plot eps frame";

    macro.sensitivity[PRE]   = true;
    macro.sensitivity[INTER] = true;
    macro.sensitivity[BATCH] = true;

    return 0;	  
  }
//--------------------------------------------------------------------------------------------------

    plot.drawDesFrame();
*/
//--------------------------------------------------------------------------------------------------
  return 0;  
}

